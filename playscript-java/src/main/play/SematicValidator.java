package play;

/**
 * 进行一些语义检查，包括：
 * 1.break只能出现在循环语句中，或case语句中；
 * 2.类的构造函数不能有返回值；
 * 3.标注左值（不标注就是右值)；
 * 4.内部类只能在类里声明，不能在别的块里。
 * 5.函数声明了返回值，就一定要有return语句。
 */
public class SematicValidator {

    // on going...
}
